import React from "react";
import { block } from "./types";

/**Create content blocks depending on the type of data received
 */
export const Blocks: React.FC<block[]> = ({ ...props }) => {
  return <div></div>;
};
